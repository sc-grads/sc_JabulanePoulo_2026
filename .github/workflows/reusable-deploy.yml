name: Deploy Local SQL

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Install SQLCMD
        run: |
          curl https://packages.microsoft.com/keys/microsoft.asc | sudo tee /etc/apt/trusted.gpg.d/microsoft.asc
          curl https://packages.microsoft.com/config/ubuntu/22.04/prod.list | sudo tee /etc/apt/sources.list.d/mssql-release.list
          sudo apt-get update
          sudo ACCEPT_EULA=Y apt-get install -y mssql-tools18
      
      - name: Debug - Check if secrets exist
        run: |
          if [ -n "${{ secrets.SQL_SERVER }}" ]; then
            echo "SQL_SERVER secret exists (value hidden)"
          else
            echo "❌ SQL_SERVER secret is MISSING"
          fi
          
          if [ -n "${{ secrets.SQL_USERNAME }}" ]; then
            echo "SQL_USERNAME secret exists (value hidden)"
          else
            echo "❌ SQL_USERNAME secret is MISSING"
          fi
          
          if [ -n "${{ secrets.SQL_PASSWORD }}" ]; then
            echo "SQL_PASSWORD secret exists (value hidden)"
          else
            echo "❌ SQL_PASSWORD secret is MISSING"
          fi
      
      - name: Execute SQL Script
        run: |
          /opt/mssql-tools18/bin/sqlcmd \
          -S "${{ secrets.SQL_SERVER }}" \
          -U "${{ secrets.SQL_USERNAME }}" \
          -P "${{ secrets.SQL_PASSWORD }}" \
          -d "Cloud-tunnel-JP" \
          -i Database/deploy.sql \
          -C
