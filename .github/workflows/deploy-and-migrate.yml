name: Deploy Database and Run Migration
run-name: "${{ github.event_name == 'push' && format('Deploy {0} - {1}', github.ref_name, github.sha) || 'Manual Deployment' }}"

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  # Deploy to DevTest Environment
  DevTest:
    uses: ./.github/workflows/reusable-deploy.yml
    with:
      environment-name: local-sql
      sql-script-path: scripts/create_database.sql
    secrets: inherit

  # Deploy to Partner Environment
  Partner:
    uses: ./.github/workflows/reusable-deploy.yml
    with:
      environment-name: partner-sql  # You'll need to create this environment in GitHub
      sql-script-path: scripts/create_database.sql
    secrets: inherit

  # Run TimesheetMigration Job after DevTest deployment
  RunMigrationJob:
    needs: DevTest  # Keep this as is, or change to Partner if needed
    runs-on: ubuntu-latest
    environment: local-sql
    if: success()
    # ... rest of RunMigrationJob steps ...

  # Run TimesheetMigration for Partner
  RunMigrationJobPartner:
    needs: Partner
    runs-on: ubuntu-latest
    environment: partner-sql
    if: success()
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install SQLCMD
        run: |
          curl https://packages.microsoft.com/keys/microsoft.asc | sudo tee /etc/apt/trusted.gpg.d/microsoft.asc
          curl https://packages.microsoft.com/config/ubuntu/22.04/prod.list | sudo tee /etc/apt/sources.list.d/mssql-release.list
          sudo apt-get update
          sudo ACCEPT_EULA=Y apt-get install -y mssql-tools18

      - name: Check and Run TimesheetMigration Job
        env:
          SERVER: ${{ secrets.SQL_SERVER }}
          PORT: ${{ secrets.SQL_PORT }}
          USERNAME: ${{ secrets.SQL_USERNAME }}
          PASSWORD: ${{ secrets.SQL_PASSWORD }}
        run: |
          echo "Checking if TimesheetMigration job exists in Partner environment..."
          # Same logic as DevTest but for partner
          # ... rest of the job execution logic ...

  # Notifications (update to include Partner)
  notify-success:
    needs:
      - DevTest
      - Partner
      - RunMigrationJob
      - RunMigrationJobPartner
    if: always() && !failure() && !cancelled()
    runs-on: ubuntu-latest
    steps:
      - name: Send Success Notification to Teams
        env:
          TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
        run: |
          if [ -n "$TEAMS_WEBHOOK_URL" ]; then
            curl -H "Content-Type: application/json" \
            -d '{
              "title": " SQL DEPLOYMENT SUCCESS",
              "text": "**Deployment completed successfully!**\n\n",
              "facts": [
                {
                  "name": " DevTest Environment",
                  "value": " Deployed Successfully"
                },
                {
                  "name": " Partner Environment",
                  "value": " Deployed Successfully"
                },
                {
                  "name": " Database",
                  "value": "TimesheetDB"
                },
                {
                  "name": " Deployed by",
                  "value": "${{ github.actor }}"
                },
                {
                  "name": " Branch",
                  "value": "${{ github.ref_name }}"
                },
                {
                  "name": " Run ID",
                  "value": "${{ github.run_id }}"
                },
                {
                  "name": " Commit",
                  "value": "${{ github.sha }}"
                }
              ],
              "themeColor": "00FF00"
            }' \
            $TEAMS_WEBHOOK_URL
          else
            echo " TEAMS_WEBHOOK_URL not set, skipping notification"
          fi